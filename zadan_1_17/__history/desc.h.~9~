#ifndef DESC
#define DESC

class Complex {
	private:
		double real;
		double imagine;
	public:
		Complex(double a, double b);
        Number();
		Complex operator+(Number);
		Complex operator-(Number);
		Complex operator*(Number);
};
#endif DESC
