#ifndef DESC
#define DESC

class Complex {
	public:
		Complex(double a, double b);
        Complex();
		Complex operator+(Complex);
		Complex operator-(Complex);
		Complex operator*(Complex);
		double real;
		double imagine;
};
#endif DESC
