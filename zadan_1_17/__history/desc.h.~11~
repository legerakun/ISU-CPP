#ifndef DESC
#define DESC

class Complex {
	private:
		double real;
		double imagine;
	public:
		Complex(double a, double b);
        Complex();
		Complex operator+(Complex);
		Complex operator-(Complex);
		Complex operator*(Complex);
		double real;
		double imagine;
};
#endif DESC
