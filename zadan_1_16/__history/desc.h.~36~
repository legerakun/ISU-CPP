#ifndef DESC
#define DESC

class Complex {
	private:
		double real;
		double imagine;
	public:
		Complex(double a, double b);
		ComplexModule();
        ComplexArgument();
		double outA();
        double outB();
};
#endif DESC
